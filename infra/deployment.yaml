apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: cncf-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo
  template:
    metadata:
      labels:
        app: demo
    spec:
      containers:
      - name: demoapp
        image: learnitguide/busapp:2.1
        ports:
        - containerPort: 8001
